@model IEnumerable<ProyectoSunshineBouquet.Models.Variedad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VariedadNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VariedadColor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VariedadNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VariedadColor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VariedadId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VariedadId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VariedadId })
        </td>
    </tr>
}

</table>
