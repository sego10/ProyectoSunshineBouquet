@model IEnumerable<ProyectoSunshineBouquet.Models.Grado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GradoNombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GradoNombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GradoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GradoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GradoId })
        </td>
    </tr>
}

</table>
